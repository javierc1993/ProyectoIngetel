import { Route } from '@angular/router';

import { PoStatusComponent } from './po-status.component';

export const poStatusRoutes: Route[] = [
    {
        path: '',
        component: PoStatusComponent,
    },
];